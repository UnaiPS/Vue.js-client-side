export class Connections{
    constructor(alias, host, port, user, pass, active){
        this.alias = alias;
        this.host = host;
        this.port = port;
        this.user = user;
        this.pass = pass;
        this.active = active;
    }
}